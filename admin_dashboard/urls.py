from django.urls import path
from . import views

urlpatterns = [
    # Jika Anda memiliki views kustom, definisikan di sini.
    # Untuk saat ini, bisa dibiarkan kosong karena semua logika ada di admin.py.
]